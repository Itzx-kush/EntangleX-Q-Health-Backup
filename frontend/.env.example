# Same-origin production default. Leave this as /api for Vercel.
# Local Vite development proxies /api to http://127.0.0.1:8000.
VITE_API_BASE=/api
# Do NOT place API tokens or backend secrets in VITE_* variables:
# VITE_* values are compiled into the browser bundle.
