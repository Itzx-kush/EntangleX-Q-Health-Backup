# Vercel production dependencies. This file is intentionally self-contained:
# Vercel parses the project-root requirements.txt directly. Quantum simulation
# and SHAP are included because they resolve and import on the current Python
# runtime; deployment still needs a plan/project eligible for large Python
# Functions when the resulting bundle exceeds the standard size.
fastapi>=0.115,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
SQLAlchemy>=2.0.36,<2.1
python-multipart>=0.0.20,<0.1
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0
scikit-learn>=1.6,<2.0
dill>=0.3.9,<0.5
psycopg[binary]>=3.2,<4.0
boto3>=1.35,<2.0
qiskit>=2.1,<3.0
qiskit-machine-learning==0.9.1
qiskit-aer>=0.17.1,<0.18
shap>=0.47,<1.0
