# Vercel production dependencies. Optional quantum/SHAP packages stay in
# backend/requirements-quantum.txt and backend/requirements-explainability.txt
# because their native footprint is too large for a reliable serverless build.
-r backend/requirements-core.txt
psycopg[binary]>=3.2,<4.0
boto3>=1.35,<2.0
